export interface FeaturesProps {
  icon: string;
  title: string;
  description: string;
}

export interface sponsorsProps {
  icon: string;
  name: string;
}

export interface ReviewProps {
  image: string;
  name: string;
  userName: string;
  comment: string;
  rating: number;
}